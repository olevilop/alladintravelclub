## Страница «Авторские туры»

По аналогии с «Активные туры».

1. **`src/data/tours.ts`** — добавить в тип `Tour` опциональный флаг `isAuthor?: boolean`.

2. **`src/pages/TourDetail.tsx`** — в сайдбаре (рядом с блоком «Активный тур») добавить условный рендер, который показывает «Авторский тур» с иконкой `Feather` из `lucide-react`, когда `tour.isAuthor === true`.

3. **`src/pages/AuthorToursPage.tsx`** — новая страница на базе `CategoryToursPage`:
   - Заголовок: «Авторские *туры*».
   - Подзаголовок: «Уникальные маршруты, разработанные нашими экспертами и проводниками клуба».
   - Хлебная крошка: «Авторские туры».
   - Источник: объединить `tours`, `japanTours`, `koreaTours`, `chinaTours`, `northKoreaTours`, `russiaTours`, `eventTours` и фильтровать по `t.isAuthor === true`.

4. **`src/App.tsx`** — добавить маршрут `/author-tours` → `<AuthorToursPage />` выше catch-all.

5. **`src/components/Footer.tsx`** — расширить тернарник в колонке «Программы»: «Авторские туры» → `<Link to="/author-tours">`.

### Логика попадания
Источник истины — флаг `isAuthor: true`. Сайдбар показывает «Авторский тур» по тому же флагу, что и фильтр страницы. После одобрения плана отдельным шагом проставим флаг тем турам, которые вы укажете.
