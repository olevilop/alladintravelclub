

## Сделать вертикальную черту чёткой и видимой

### Файл: `src/pages/SpecialOffersPage.tsx`

Заменить `bg-border/30` на `bg-border` в элементе-разделителе (строка ~68). Это уберёт прозрачность и сделает линию полностью видимой.

