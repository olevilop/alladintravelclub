

## Миниатюры под Hero + удаление галереи ниже

### Что делаем
1. Под секцией Hero (большое фото) добавить ряд из 5 маленьких превью-фото. Клик по превью меняет главное фото в Hero.
2. Убрать существующий блок галереи (большое фото + сетка миниатюр 3 колонки) из основного контента.

### Проблема: в галереях сейчас по 3 фото
Нужно расширить массивы `gallery` до 5 изображений в `src/data/tours.ts`, добавив дополнительные фото из имеющихся ассетов.

### Изменения

**`src/data/tours.ts`** — расширить каждый `gallery` до 5 элементов, добавив изображения из других туров того же региона или общие.

**`src/pages/TourDetail.tsx`**:
1. Состояние `galleryIndex` управляет фото в Hero — `tour.gallery[galleryIndex]` вместо `tour.image`
2. После `</section>` Hero добавить ряд из 5 миниатюр:
   ```
   <div className="container mx-auto px-6 -mt-12 relative z-10">
     <div className="grid grid-cols-5 gap-2">
       {tour.gallery.slice(0,5).map(...)}
     </div>
   </div>
   ```
   Каждая миниатюра — `aspect-[16/9]`, рамка `border-2 border-primary` при активной
3. Удалить блок Gallery (строки ~87–128) — большое фото с навигацией и сетку превью

### Файлы
- `src/data/tours.ts` — расширить gallery до 5 фото
- `src/pages/TourDetail.tsx` — миниатюры под Hero, удаление старой галереи

