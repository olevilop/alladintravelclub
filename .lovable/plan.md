

## Добавить карточку «Мальдивы» первой на страницу спецпредложений

### Изменение

**Файл: `src/pages/SpecialOffersPage.tsx`**

Добавить перед циклом `allTours.map(...)` отдельную карточку-ссылку на `/maldives` в том же визуальном стиле, что и остальные карточки туров. Карточка будет содержать:

- Изображение Мальдив (то же фото из MaldivesPage hero)
- Заголовок: «Мальдивы»
- Описание: «Там где время останавливается. Бирюзовый океан, виллы над водой и абсолютная приватность»
- Вместо блока дней/цены — регион «Мальдивы» и пометка «Индивидуальный подбор»

Ссылка ведёт на `/maldives` вместо `/tour/:id`.

### Технические детали
- Карточка размещается внутри `<section>` перед `{allTours.map(...)}` с тем же классом `block bg-card overflow-hidden` и внутренней структурой (image 300px / text / divider / info 240px)
- Используются те же иконки `MapPin`, `Banknote` из lucide-react
- Один файл, одно изменение

