## Изменения в `src/pages/cruises/LinerDetailPage.tsx` (блок «Технические характеристики», ~строки 308–326)

1. Ширина карточки: `max-w-[354px]` → `max-w-[334px]` (−20px).
2. Размер значения (золотой текст): `text-base` → `text-lg` на `<dd>`.
3. Пунктирная линия — выровнять по базовой линии текста (низ), а не по центру:
   - У строки спецификации заменить `items-baseline` на `items-end`.
   - У `<span>` с пунктиром убрать `translate-y-[-4px]`, добавить небольшой отступ снизу (`mb-1`), чтобы точки шли по нижней границе текста.