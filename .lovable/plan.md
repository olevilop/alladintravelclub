## Страница «Оздоровительные программы»

По аналогии с «Активные», «Авторские», «Корпоративные туры».

1. **`src/data/tours.ts`** — добавить в тип `Tour` опциональный флаг `isWellness?: boolean`.

2. **`src/pages/TourDetail.tsx`** — в сайдбаре (после блока «Корпоративный тур») добавить условный рендер «Оздоровительный тур» с иконкой `HeartPulse` из `lucide-react`, когда `tour.isWellness === true`.

3. **`src/pages/WellnessToursPage.tsx`** — новая страница на базе `CategoryToursPage`:
   - Заголовок: «Оздоровительные *программы*».
   - Подзаголовок: «Спа, термы, ретриты и программы восстановления в лучших курортных направлениях».
   - Хлебная крошка: «Оздоровительные программы».
   - Источник: объединить `tours`, `japanTours`, `koreaTours`, `chinaTours`, `northKoreaTours`, `russiaTours`, `eventTours` и отфильтровать по `t.isWellness === true`.

4. **`src/App.tsx`** — добавить маршрут `/wellness-tours` → `<WellnessToursPage />` выше catch-all.

5. **`src/components/Footer.tsx`** — расширить тернарник в колонке «Программы»: «Оздоровительные» → `<Link to="/wellness-tours">`.

### Логика попадания
Источник истины — флаг `isWellness: true`. Сайдбар и фильтр страницы используют один и тот же флаг. После одобрения плана отдельным шагом проставим флаг турам, которые вы укажете.
