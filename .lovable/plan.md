

## Изменить порядок: сначала цифра, потом «день»

### Изменение в `src/pages/TourDetail.tsx`

В блоке маршрута поменять текст внутри кружка с `День {day.day}` на `{day.day} день`.

### Файлы
- `src/pages/TourDetail.tsx` — одна строка с текстом кружка

