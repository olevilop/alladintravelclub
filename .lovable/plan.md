## Задача
В подвале сайта в колонке «Полезное» пункт «Спецпредложения» сейчас неактивная кнопка. Нужно сделать её рабочей ссылкой на страницу `/special-offers`.

## Изменения
**Файл:** `src/components/Footer.tsx`

Расширить условие в рендере `footerLinks` (строки ~132–141), добавив `Спецпредложения` к списку пунктов, которые рендерятся как `<Link>`:

- `Политика конфиденциальности` → `/privacy`
- `Событийный туризм` → `/event-tours`
- `Спецпредложения` → `/special-offers` (новое)

Маршрут `/special-offers` уже зарегистрирован в `App.tsx` — других правок не требуется.
