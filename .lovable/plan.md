

## Расширить блок «Мы в соцсетях»

### Что делаем
Блок соцсетей слишком узкий с `auto`. Зададим ему фиксированную минимальную ширину, чтобы он выглядел пропорциональнее.

### Изменения в `src/components/NewsletterSocial.tsx`
- Строка 22: сетка `md:grid-cols-[1fr_auto]` → `md:grid-cols-[1fr_240px]` — блок соцсетей получит фиксированную ширину 240px вместо сжатия до минимума

### Файл
- `src/components/NewsletterSocial.tsx`

