

## Укоротить кнопки соцсетей вдвое

### Что делаем
Кнопки сейчас растянуты на всю ширину блока (`justify-center` + блочный `flex-col`). Нужно убрать растяжение — сделать кнопки по ширине контента, добавив `self-start` или `w-fit`.

### Изменения в `src/components/NewsletterSocial.tsx`
- Убрать `justify-center` из классов каждой кнопки (или оставить)
- Добавить `w-1/2` к каждой кнопке — ровно половина ширины контейнера

### Файл
- `src/components/NewsletterSocial.tsx` — добавить `w-1/2` к трём кнопкам соцсетей

