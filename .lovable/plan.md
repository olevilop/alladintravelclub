## Цель
Заменить текст в блоке «О путешествии» страницы тура «Тропические ритмы Сейшельских островов» на новые 3 абзаца.

## Изменения

### 1. `src/pages/TourDetail.tsx` (строка 109)
Сейчас описание выводится одним `<p>`. Заменить на разбиение по `\n`, чтобы поддерживать многоабзацный текст из данных:

```tsx
<div className="space-y-4 text-foreground/80 leading-relaxed text-base md:text-lg">
  {tour.description.split('\n').filter(Boolean).map((p, i) => (
    <p key={i}>{p}</p>
  ))}
</div>
```

### 2. `src/data/tours.ts` — `seychelles-tropical-rhythms.description`
Заменить текущий однострочный текст на 3 абзаца, разделённые `\n`:

> «Это не просто круиз. …»
> «Представьте утро, когда ваша яхта скользит …»
> «По пути вы исследуете пролив Сент-Анн …»

## Результат
В блоке «О путешествии» появятся 3 абзаца предоставленного текста с правильными отступами. Изменение в `TourDetail` обратно совместимо — однострочные описания других туров продолжат работать как раньше.
