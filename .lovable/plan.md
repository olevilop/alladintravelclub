## Новый тур: Новый год в Антарктиде на Scenic Eclipse 6*

Создаю экспедиционный круиз `antarctica-newyear-2027` по образцу `brazil-scenic-eclipse-2027`. Отдельные компоненты не нужны — структура страницы и стили уже общие (`TourDetail.tsx`), достаточно добавить данные.

### Изменения

**1. `src/data/tours.ts`** — добавить новый объект в массив `tours` (рядом с brazil-scenic-eclipse-2027):

- `id: "antarctica-newyear-2027"`
- `name: "Новый год в Антарктиде на Scenic Eclipse 6*"` (короткое — для карточки)
- `region: "Антарктида"`, `category: "expedition"`, `days: 13`
- `price: "от €19 765"`
- `badge: "🎄 НОВЫЙ ГОД"` — отрисуется внизу сайдбара (логика уже есть в TourDetail)
- `image` + `gallery` — placehold.co плейсхолдеры (Антарктида / Scenic Eclipse / Ушуайя / айсберги / пингвины)
- `subtitle`, `description` — тексты из брифа
- `itinerary` — 8 пунктов (дни 1–4 отдельно, дни 5–10 одним блоком, дни 11–12 одним, день 13 отдельно), плюс финальное примечание о возможном изменении маршрута
- `included` / `notIncluded` — списки из брифа
- `paymentTerms` — 30% предоплата, финал за 60 дней
- `cancellationTerms` — 150+/149–91/90–51/≤50
- `startDates: ["28.12.2026 — 09.01.2027"]`
- `shipName: "Scenic Eclipse 6*"`, `shipImage` — тот же URL, что у Бразилии
- `extras` — переиспользую текст «о судне» из brazil-scenic-eclipse-2027
- `cabinPricing` — 6 доступных кают (цены из брифа) + остальные категории как `soldOut: true` (по аналогии с Бразилией: Делюкс CA 5п, Гранд CD 5п, Делюкс AA 7п и т.п.)

**2. `src/data/tourRoutes.ts`** — добавить маршрут для `antarctica-newyear-2027`: Буэнос-Айрес → Ушуайя → пр. Дрейка → Антарктический п-ов → Порт-Локрой → Лемэр-канал → пр. Дрейка → Ушуайя → Буэнос-Айрес. Тогда в сайдбаре отрисуется строка маршрута и компонент `RouteMap`.

### Автоматические эффекты (без правок)

- **Главная → «Экспедиционные круизы»**: `ExpeditionCruisesPage` и соответствующий блок на главной фильтруют `tours` по `category === "expedition"` → новый тур появится рядом с Сейшелами и Бразилией.
- **«Спецпредложения»**: `SpecialOffers.tsx` рандомно выбирает 8 туров из всего пула — новый тур автоматически попадёт в ротацию (отдельный `specialOfferTag` в коде не используется, поэтому добавлять его не нужно).
- **Страница `/tours/antarctica-newyear-2027`**: рендерится через общий `TourDetail.tsx` — Hero, галерея-стрип, описание, маршрут, оплата, аннуляция, экстра «о судне», включено/не включено, сайдбар (info + RouteMap + cabinPricing + ship-картинка + booking form + `ExpeditionManagerCard` Виктория), FAQ, SimilarTours, SpecialOffers.
- **Бейдж «🎄 НОВЫЙ ГОД»**: отрисуется в карточке быстрой инфы сайдбара через существующее поле `tour.badge`.
- **Менеджер Виктория**: т.к. `region !== "Япония" && region !== "Южная Корея"`, TourDetail уже подставит `<ExpeditionManagerCard />`.

### Замечания

- Континент «Антарктида» уже замаплен в `regionToContinent` — правок мэппинга не нужно.
- TourCard на главной берёт страну из `region` — отобразится «Антарктида» в правом верхнем углу автоматически. Отдельной иконки 🎄 в левом верхнем углу карточки текущий `TourCarousel`/`TourCard` не поддерживает; на детальной странице бейдж показывается через `tour.badge`. Если нужен значок 🎄 именно на карточке-превью — это потребует доработки `TourCard` (могу добавить в follow-up).
