## Удалить тур «Экспедиция к Южному полюсу»

### Что будет сделано

1. **src/data/tours.ts** — удалить объект тура `id: "south-pole-expedition"` (строки 447–505).
2. **src/data/tourRoutes.ts** — удалить запись маршрута `"south-pole-expedition"` (строки 20–28).

### Результат
- Карточка тура автоматически исчезнет с главной страницы (блок «Экспедиционные круизы»).
- Тур пропадёт со страницы `/expedition-cruises`, из блока «Спецпредложения» и из списка похожих туров.
- Переход на `/tour/south-pole-expedition` вернёт NotFound.

### Что не трогаем
- Импорты `antarcticaImg`, `tourPenguins`, `tourExpedition` оставляем (используются в других турах).
- Остальные туры, стили, шрифты и разделы сайта.