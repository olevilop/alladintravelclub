## Страница «Корпоративные туры»

По аналогии с «Активные туры» и «Авторские туры».

1. **`src/data/tours.ts`** — добавить в тип `Tour` опциональный флаг `isCorporate?: boolean`.

2. **`src/pages/TourDetail.tsx`** — в сайдбаре (рядом с блоками «Активный тур» и «Авторский тур») добавить условный рендер «Корпоративный тур» с иконкой `Briefcase` из `lucide-react`, когда `tour.isCorporate === true`.

3. **`src/pages/CorporateToursPage.tsx`** — новая страница на базе `CategoryToursPage`:
   - Заголовок: «Корпоративные *туры*».
   - Подзаголовок: «Программы для команд и партнёров — выездные мероприятия, тимбилдинги и поощрительные поездки».
   - Хлебная крошка: «Корпоративные туры».
   - Источник: объединить `tours`, `japanTours`, `koreaTours`, `chinaTours`, `northKoreaTours`, `russiaTours`, `eventTours` и отфильтровать по `t.isCorporate === true`.

4. **`src/App.tsx`** — добавить маршрут `/corporate-tours` → `<CorporateToursPage />` выше catch-all.

5. **`src/components/Footer.tsx`** — расширить тернарник в колонке «Программы»: «Корпоративные туры» → `<Link to="/corporate-tours">`.

### Логика попадания
Источник истины — флаг `isCorporate: true`. Сайдбар показывает «Корпоративный тур» по тому же флагу, что и фильтр страницы. После одобрения плана отдельным шагом проставим флаг тем турам, которые вы укажете.
