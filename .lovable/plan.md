

## Блок «Менеджер по Японии» на страницах туров по Японии

Добавить новый компонент-карточку, отображаемую в сайдбаре страницы тура **только для японских туров** (по `tour.region === "Япония"`), сразу после формы бронирования.

### Что будет сделано

**1. Создание компонента `src/components/TourManagerCard.tsx`**

Карточка в стиле существующих sidebar-блоков (`bg-card border border-border p-6`):
- Заголовок: «Менеджер по Японии»
- Три кнопки-ссылки в стиле сайта:
  - **Telegram** — ссылка `https://t.me/+79025240653`, иконка из lucide-react (`MessageCircle` или SVG Telegram)
  - **WhatsApp** — ссылка `https://wa.me/79025240653`, иконка
  - **Телефон** — ссылка `tel:+79025240653`, текст `+7(902)524-06-53`, иконка `Phone`
- Кнопки оформлены как компактные строки с иконкой и текстом, открываются в новом окне

**2. Обновление `src/pages/TourDetail.tsx`**

- Импорт `TourManagerCard`
- После `<TourBookingForm ... />` (строка 341) добавить условный рендер:
  ```
  {tour.region === "Япония" && <TourManagerCard />}
  ```

### Результат

На всех страницах японских туров в правом сайдбаре под формой бронирования появится блок с контактами менеджера.

