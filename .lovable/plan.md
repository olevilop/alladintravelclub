

## Добавление блока «Стоимость экскурсий» в сайдбар

### Что будет сделано

**1. `src/data/tours.ts`** — добавить новое необязательное поле в интерфейс `Tour`:
```ts
excursionPricing?: {
  excursions: {
    name: string;
    prices: { label: string; value: string }[];
  }[];
};
```

Добавить данные `excursionPricing` в тур `japan-fuji-lakes` с тремя экскурсиями:
- Вечерний Токио (Взрослые: 190$, Дети 6–11: 160$, Дети 0–3: бесплатно; 4–5 лет: 20$)
- Традиции Токио (Взрослые: 210$, Дети 6–11: 170$, Дети 0–5: бесплатно без места и питания)
- Нара и Осака (Взрослые: 220$, Дети 6–11: 190$, Дети 0–5: бесплатно без питания)

**2. `src/pages/TourDetail.tsx`** — после блока «Стоимость тура» (строка 292) вставить аналогичный блок с `Select` для выбора экскурсии по названию. При выборе экскурсии отображается список цен в формате «ключ — значение». Добавить `useState` для `selectedExcursion`.

### Структура блока
```text
┌─ СТОИМОСТЬ ЭКСКУРСИЙ ────┐
│ [▼ Вечерний Токио       ] │
│                           │
│ Взрослые          190 $   │
│ Дети 6–11 лет     160 $   │
│ Дети 0–3 года  бесплатно  │
│ ...                       │
└───────────────────────────┘
```

