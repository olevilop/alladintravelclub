

## Менеджер по Южной Корее на страницах туров

На страницах туров по Японии уже отображается карточка `TourManagerCard` (Елена) — рендерится в `TourDetail.tsx` условием `tour.region === "Япония"`.

### Что уточнить

Контакты менеджера по Корее не указаны. Предполагаю один из вариантов:
1. **Те же контакты, что у Елены** (Telegram/WhatsApp/телефон +7 902 524-06-53), но имя/роль изменить на «Менеджер по Южной Корее».
2. **Использовать ту же карточку Елены без изменений** — она ведёт оба направления.
3. **Другое имя и контакты** — пришлёте отдельно.

По умолчанию в плане ниже использую вариант 2 (та же карточка Елены) — если нужен другой вариант, напишите и я скорректирую.

### Изменения

**`src/pages/TourDetail.tsx`** (строка ~333): расширить условие рендера `TourManagerCard`, чтобы карточка показывалась и для региона «Южная Корея»:

```tsx
{(tour.region === "Япония" || tour.region === "Южная Корея") && <TourManagerCard />}
```

Карточка появится в боковой панели сразу после формы бронирования на всех 5 страницах корейских туров (`korea-jeju-island`, `korea-hanok-culture`, `korea-busan-coastal`, `korea-temple-stay` и других туров с `region === "Южная Корея"`).

### Что НЕ меняется

- Сам компонент `TourManagerCard.tsx` (контакты Елены)
- Расположение блока (ниже формы бронирования)
- Поведение для Японии — без изменений

