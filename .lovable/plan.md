## План: Страница «Круизы по Австралии и Океании»

### Цель
Обновить существующую страницу `/cruises/oceania`, приведя её к тому же шаблону и логике, что и страница «Круизы по Арктике» (`ArcticCruisesPage`).

### Шаги

1. **Генерация hero-изображения**
   - Сгенерировать изображение для hero-секции страницы «Круизы по Австралии и Океании» (тропические острова, коралловые рифы, Австралия/Океания).
   - Сохранить в `src/assets/oceania-hero.jpg`.

2. **Обновление `src/pages/cruises/OceaniaCruisesPage.tsx`**
   - Импортировать сгенерированное hero-изображение.
   - Заменить пустой массив `tours={[]}` на фильтрацию по точному региону `"Австралия и Океания"` (по аналогии с Арктикой/Антарктидой).
   - Установить:
     - `title`: «Круизы в Австралию и Океанию»
     - `subtitle`: «Тропические острова и коралловые рифы»
     - `breadcrumbLabel`: «Австралия и Океания»
     - `fallbackHeroImage`: импортированное изображение
     - `hideSpecialOfferTag`

3. **Обновление `src/data/tours.ts` (regionToContinent)**
   - Добавить маппинг `"Австралия и Океания": "Австралия и Океания"` в `regionToContinent`, чтобы будущие туры с таким регионом корректно привязывались к континенту.

### Что не меняется
- Маршрут `/cruises/oceania` уже прописан в `App.tsx`.
- Шаблон `CategoryToursPage` используется без изменений.
- Другие страницы континентов не затрагиваются.
