

## Убрать кнопки фильтрации регионов в блоке «Экспедиционные круизы»

**Что делаем:** Удаляем блок кнопок «Все / Арктика / Антарктида / Африка / ...» из секции каталога туров на главной странице.

**Файл: `src/components/ToursSection.tsx`**

1. Удалить массив `regions` (строка ~9)
2. Удалить состояние `activeRegion` (useState)
3. Удалить вычисление `filtered` — использовать `tours` напрямую
4. Удалить JSX-блок с кнопками фильтрации (div с `flex-wrap justify-center gap-3`)
5. Заменить `filtered` на `tours` в `TourCarousel` и условии для классических круизов

