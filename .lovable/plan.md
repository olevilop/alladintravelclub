

## Выровнять текст в Hero-секции по центру

**Файл: `src/components/HeroSection.tsx`**

Строка 59: изменить `items-start` на `items-center` и добавить `text-center` для центрирования контента по горизонтали.

Строка 69: добавить `text-center` к классам заголовка h1.

Строка 79: добавить `text-center` к классам подзаголовка.

Строка 90: перенести кнопку «Потри лампу» — заменить `left-6 sm:left-12 lg:left-24` на `left-1/2 -translate-x-1/2` для центрирования.

Четыре точечных правки в одном файле.

