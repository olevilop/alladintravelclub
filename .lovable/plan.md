

## Добавить блок «Дополнительно» между маршрутом и «Что включено»

### Что будет сделано

Добавить новое необязательное поле `extras` в модель тура и отобразить его на странице тура между секцией «Маршрут по дням» и секцией «Что включено».

### Изменения по файлам

**1. `src/data/tours.ts`**

- Добавить в интерфейс `Tour` необязательное поле `extras?: string` 
- В туре `japan-onsen-trail` заполнить `extras` текстом о Диснейленде и примечаниями (Диснейленд, Диснейси, стоимость доп. экскурсий, корректировка маршрута)

**2. `src/pages/TourDetail.tsx`**

- Между блоком Itinerary и блоком Included/Not Included добавить условный блок: если `tour.extras` существует, отобразить его в `motion.div` с заголовком «Дополнительно» и текстом, разбитым по абзацам (через `split('\n')`)

