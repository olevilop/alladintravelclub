## Задача
В подвале сайта в колонке «Программы» фраза «Событийный туризм» сейчас является неактивной кнопкой. Нужно сделать её рабочей ссылкой, ведущей на страницу `/event-tours`.

## Изменения
**Файл:** `src/components/Footer.tsx`

В блоке рендера колонок `footerLinks` (строки ~130–147) добавить условие для пункта «Событийный туризм» — рендерить его как `<Link to="/event-tours">` (по аналогии с «Политика конфиденциальности»), со скроллом наверх по клику.

Логика:
- `Политика конфиденциальности` → `/privacy` (как сейчас)
- `Событийный туризм` → `/event-tours` (новое)
- Остальные пункты — остаются обычными кнопками-заглушками

Других правок не требуется — маршрут `/event-tours` уже зарегистрирован в `App.tsx`.
