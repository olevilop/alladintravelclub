

## Удалить анимированную стрелку вниз из Hero

**Файл: `src/components/HeroSection.tsx`**

Удалить блок с `<ChevronDown>` (строки ~114–122) — анимированную стрелку внизу Hero-секции. Также убрать импорт `ChevronDown` из lucide-react, если он больше нигде не используется.

Одна правка, один файл.

