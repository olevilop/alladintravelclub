## Страница «Круизы по Арктике»

Маршрут `/cruises/arctic` уже существует (`ArcticCruisesPage.tsx`), а ссылки в шапке и подвале уже на него указывают. Нужно привести страницу к той же логике, что и «Круизы по Антарктиде».

### Изменения

#### `src/pages/cruises/ArcticCruisesPage.tsx`
- Импортировать `tours` из `@/data/tours`.
- Передавать в `CategoryToursPage` отфильтрованные туры: `tours.filter(t => t.region === "Арктика")` — попадут все туры (как из «Классических», так и из «Экспедиционных круизов»), у которых в карточке указан материк «Арктика».
- `title`: `Круизы по <span className="italic">Арктике</span>`.
- `subtitle`: «Путешествия к вершине мира».
- `breadcrumbLabel="Круизы по Арктике"` — хлебные крошки будут «Главная › Круизы по Арктике», «Главная» кликабельна.
- Добавить `hideSpecialOfferTag`, как на странице Антарктиды.

### Навигация
Ссылки уже настроены и не требуют правок:
- Шапка → «Круизы → Арктика» → `/cruises/arctic`.
- Подвал → «Круизы → Арктика» → `/cruises/arctic`.

### Примечание
Сейчас в `src/data/tours.ts` нет туров с `region: "Арктика"` — список на странице будет пустым до добавления соответствующих туров. Структура и оформление полностью совпадут со страницей «Круизы по Антарктиде».