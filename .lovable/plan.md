## Цель
На странице «Экспедиционные круизы»:
1. Вернуть отображение нижнего бейджа спецпредложения (как было раньше).
2. Убрать верхний бейдж `tour.badge` («🇷🇺 РУССКАЯ ГРУППА» / «🎄 НОВЫЙ ГОД»), который выводится в текстовой колонке над названием тура.

## Изменения

1. **`src/pages/CategoryToursPage.tsx`**
   - Заменить пропс `hideSpecialOfferTag` на `hideBadge?: boolean`.
   - Условие нижнего блока спецпредложения вернуть к исходному: `getSpecialOfferLabel(tour.specialOfferTag) && (...)`.
   - Обернуть верхний бейдж проверкой: `!hideBadge && tour.badge && (...)`.

2. **`src/pages/ExpeditionCruisesPage.tsx`**
   - Заменить проп `hideSpecialOfferTag` на `hideBadge`.

Другие категорийные страницы не затрагиваются — у них бейдж сверху по-прежнему отображается.
