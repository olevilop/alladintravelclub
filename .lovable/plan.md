

## Добавить хлебные крошки на страницы категорий

Хлебные крошки уже есть на страницах категорий (`CategoryToursPage.tsx`), но нужно убедиться, что они корректно отображают `Главная > Название категории` и что «Главная» ведёт на `/`.

**Файл: `src/pages/CategoryToursPage.tsx`** — крошки уже подключены через `<Breadcrumbs items={[{ label: breadcrumbLabel }]} />`. Компонент `Breadcrumbs` автоматически добавляет «Главная» с навигацией на `/`. Никаких изменений не требуется — всё уже работает.

Если нужно — могу открыть страницу категории в браузере и проверить визуально.

