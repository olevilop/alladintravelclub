

## Блок «Отели» с выбором через Select

### Что будет сделано

**`src/pages/TourDetail.tsx`** (строки 260-290) — заменить таблицу на:

1. Компонент `Select` для выбора отеля (Отели 2–3*, Отели 3* комфорт, Отели 4*)
2. `useState` для хранения выбранного отеля (по умолчанию — первый)
3. После выбора — отображение списка цен выбранного отеля в виде компактных строк (категория → цена)

Структура:
```
┌─ Отели ──────────────────┐
│ [▼ Отели 2–3*          ] │
│                          │
│ ½ TWIN          2 990 $  │
│ Доплата за SGL    650 $  │
│ Доп. кровать      N/A    │
│ ...                      │
└──────────────────────────┘
```

Данные берутся из существующего `tour.hotelPricing` без изменения структуры данных.

