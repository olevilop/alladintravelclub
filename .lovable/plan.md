

## Растянуть описание компании на половину страницы

**Файл: `src/components/Footer.tsx`**

В строке 52 заменить класс `max-w-md` на `max-w-2xl` — это увеличит максимальную ширину текстового блока с ~28rem до ~42rem, что позволит тексту занимать примерно половину ширины страницы.

