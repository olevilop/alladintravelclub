## Удалить тур «Сафари в Серенгети Deluxe»

### Что будет сделано

1. **src/data/tours.ts** — удалить объект тура `id: "serengeti-safari"` (строки 559–601).
2. **src/data/tourRoutes.ts** — удалить запись маршрута `"serengeti-safari"` (строки 36–43).

### Результат
- Карточка тура автоматически исчезнет с главной страницы (блок «Экспедиционные круизы»).
- Тур пропадёт со страницы `/expedition-cruises`, из блока «Спецпредложения» и из списка похожих туров.
- Переход на `/tour/serengeti-safari` вернёт NotFound.

### Что не трогаем
- Импорт `tourSafari` оставляем (могут быть другие ссылки).
- Остальные туры, стили, шрифты и разделы сайта.