

## Выровнять высоту блоков рассылки и соцсетей

### Изменение в `src/components/NewsletterSocial.tsx`
- Строка ~22: заменить `items-start` на `items-stretch` в контейнере `flex`, чтобы оба блока растягивались на одинаковую высоту

### Файл
- `src/components/NewsletterSocial.tsx`

